# Cursor Settings UI — Personal Conventions

> **Optional.** The operating model (Planner/Executor roles, workflow, scratchpad conventions) now lives
> in `cursor/rules/operating-model.mdc` and syncs automatically. This file is only for personal
> conventions you want applied globally across all projects — things that don't belong in shared rules.
>
> If you use this, paste it into **Cursor Settings > Rules, Skills...**
> Last updated: 2026-04-09.

---

```
## Lessons (User Specified)
- Include info useful for debugging in program output.
- Read a file before editing it.
- If vulnerabilities appear in terminal, run npm audit before proceeding.
- Always ask before using the -force git command.
```
